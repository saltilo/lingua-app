import type { SxProps, Theme } from "@mui/material";

export const cardStyle: SxProps<Theme> = {
  minWidth: 250,
  maxWidth: 300,
  p: 3,
  borderRadius: 4,
  textAlign: "center",
  background: "#D9E0FF",
  flex: "0 0 auto",
};
